# Database Configuration
DATABASE_URL=postgresql://aura_user:password@localhost:5432/aura_journal
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=secure_password
MONGODB_URL=mongodb://${MONGO_ROOT_USER}:${MONGO_ROOT_PASSWORD}@localhost:27017

# Service URLs
NLP_AGENT_URL=http://localhost:8001/analyze

# Google Cloud Platform
GOOGLE_APPLICATION_CREDENTIALS=/path/to/gcp-service-account-key.json
GCP_KEYFILE_PATH=/path/to/gcp-service-account-key.json

# Development Settings
DEBUG=true
LOG_LEVEL=INFO

# Docker PostgreSQL (used by docker-compose)
POSTGRES_DB=aura_journal
POSTGRES_USER=aura_user
POSTGRES_PASSWORD=secure_password
